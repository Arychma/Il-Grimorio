{% macro cardGrid(collectionName, tagFilter=null) %}
  {# 1. Recuperiamo la collezione dall'oggetto globale #}
  {% set allItems = collections[collectionName] or [] %}
  
  {# 2. Decidiamo se filtrare o usare tutto #}
  {% if tagFilter %}
    {% set finalItems = allItems | filterByTag(tagFilter) %}
  {% else %}
    {% set finalItems = allItems %}
  {% endif %}

  <div class="card-grid">
    {% for item in finalItems %}
      <article class="card">
        <a href="{{ item.url }}">
          <div class="card-body">
            <h3 class="card-title">
              {{ item.data.title or item.title or "Senza Titolo" }}
            </h3>
            
            {% set info = item.data if item.data else item %}
            
            <div class="card-meta">
              {% if info.type %}
                <span class="tag-type">{{ info.type }}</span>
              {% endif %}
              
              {% if info.region %}
                <span class="tag-region">{{ info.region }}</span>
              {% endif %}

              {% if info.status %}
                <span class="status {{ info.status | lower }}">{{ info.status }}</span>
              {% endif %}
            </div>

            {% if info.description %}
              <p class="card-text">{{ info.description }}</p>
            {% endif %}
          </div>
        </a>
      </article>
    {% else %}
      <p class="debug-msg">Nessun elemento presente in <em>{{ collectionName }}</em>.</p>
    {% endfor %}
  </div>
{% endmacro %}